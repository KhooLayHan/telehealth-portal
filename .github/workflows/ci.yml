name: CI
 
on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]
 
jobs:
  # backend:
    # name: Backend — Build & Test
    # runs-on: ubuntu-latest
    # steps:
    #   - uses: actions/checkout@v4
 
    #   - name: Setup .NET
    #     uses: actions/setup-dotnet@v4
    #     with:
    #       dotnet-version: '9.0.x'
 
    #   - name: Restore dependencies
    #     run: dotnet restore apps/api/src/TeleHealth.Api/TeleHealth.Api.csproj
 
    #   - name: Build
    #     run: dotnet build apps/api/src/TeleHealth.Api/TeleHealth.Api.csproj --no-restore
 
    #   - name: Unit Tests
    #     run: dotnet test apps/api/tests/TeleHealth.UnitTests/TeleHealth.UnitTests.csproj --no-build --verbosity normal
 
    #   - name: Integration Tests
    #     run: dotnet test apps/api/tests/TeleHealth.IntegrationTests/TeleHealth.IntegrationTests.csproj --verbosity normal
 
  frontend:
    name: Frontend — Build & Lint
    runs-on: ubuntu-latest    
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
 
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      
      - uses: actions/cache@v4
        name: Configure Bun caching
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - name: Install dependencies
        run: bun ci
 
      - name: Check formatting and linting 
        run: bun run check

      - name: Test
        run: bun run test

      - name: Build
        run: bun run build