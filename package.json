{
  "name": "telehealth",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun run --cwd frontend dev && bun run --cwd backend dev",
    "build": "bun run --cwd frontend build && bun run --cwd backend build",
    "check": "ultracite check",
    "format": "bun run --cwd frontend fix && bun run --cwd backend format",
    "test": "bun run --cwd frontend test && bun run --cwd backend test",
    "lefthook": "lefthook install",
    "fix": "ultracite fix"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.5",
    "lefthook": "^2.1.3",
    "ultracite": "7.2.5"
  }
}
